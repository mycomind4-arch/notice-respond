/**
 * Domain layer public API.
 *
 * Import from `@/domain` rather than individual files:
 *   import type { MailJob, Document } from "@/domain";
 *   import { canTransitionTo } from "@/domain";
 */

export * from "./models";
export * from "./status-mapping";

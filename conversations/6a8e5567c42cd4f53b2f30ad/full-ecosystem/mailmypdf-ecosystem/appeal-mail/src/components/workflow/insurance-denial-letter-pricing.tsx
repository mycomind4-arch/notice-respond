import { INSURANCE_DENIAL_LETTER_PRICING as p } from "@/domain/insurance-denial-letter-gold";
export function InsuranceDenialLetterPricing(){const start=p.preparationFee+p.includedResponsePages*p.responsePagePrice+p.standardMail;return <section className="mx-auto max-w-6xl px-6 py-10"><div className="rounded-3xl border border-rule bg-paper-deep p-8"><div className="text-xs font-semibold uppercase tracking-[0.2em] text-muted-foreground">Transparent pricing</div><h2 className="mt-3 font-serif text-3xl">Starting at ${start.toFixed(2)}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-muted-foreground">Includes preparation, up to {p.includedResponsePages} response sheets, and standard mailing. Additional response sheets are ${p.responsePagePrice.toFixed(2)} each; supporting-document sheets are ${p.supportingPagePrice.toFixed(2)} each.</p><div className="mt-6 grid gap-3 md:grid-cols-3"><div className="rounded-2xl border border-rule bg-paper p-5"><div className="text-xs uppercase tracking-widest text-muted-foreground">Standard</div><div className="mt-2 text-xl font-semibold">${p.standardMail.toFixed(2)}</div></div><div className="rounded-2xl border border-rule bg-paper p-5"><div className="text-xs uppercase tracking-widest text-muted-foreground">Certified</div><div className="mt-2 text-xl font-semibold">${p.certifiedMail.toFixed(2)}</div></div><div className="rounded-2xl border border-rule bg-paper p-5"><div className="text-xs uppercase tracking-widest text-muted-foreground">Registered</div><div className="mt-2 text-xl font-semibold">${p.registeredMail.toFixed(2)}</div></div></div><p className="mt-5 text-xs text-muted-foreground">Your exact price is calculated from the final approved packet before payment. Supporting evidence is never silently treated as free.</p></div></section>}

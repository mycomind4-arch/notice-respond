import { COURT_RULING_PRICING as P } from "@/domain/court-ruling-gold";
export function CourtRulingPricing(){return <section className="rounded-2xl border border-rule bg-paper-deep p-6"><h2 className="font-serif text-2xl">Transparent pricing</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The final price is based on the approved physical packet, not an arbitrary flat fee.</p><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["Preparation",`$${P.preparationFee.toFixed(2)}`],["Included response",`${P.includedResponsePages} sheets`],["Extra response",`$${P.responsePagePrice.toFixed(2)}/sheet`],["Supporting documents",`$${P.supportingPagePrice.toFixed(2)}/sheet`],["Standard mail",`$${P.standardMail.toFixed(2)}`],["Certified mail",`$${P.certifiedMail.toFixed(2)}`],["Registered mail",`$${P.registeredMail.toFixed(2)}`],["Large packet",`+$${P.largePacketFee.toFixed(2)} at ${P.largePacketThresholdSheets}+ sheets`]].map(([label,value])=><div key={label} className="rounded-xl border border-rule bg-paper p-4"><div className="text-xs text-muted-foreground">{label}</div><div className="mt-1 font-semibold">{value}</div></div>)}</div><p className="mt-5 text-xs leading-5 text-muted-foreground">Supporting evidence is priced by physical sheet so a larger record costs more to fulfill. The approved packet total is the amount sent to Stripe.</p></section>}

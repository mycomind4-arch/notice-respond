import { PRIOR_AUTHORIZATION_DENIAL_PRICING as p } from "@/domain/prior-authorization-denial-gold";
export function PriorAuthorizationDenialPricing(){return <section className="mx-auto mt-8 max-w-5xl rounded-2xl border border-rule bg-paper-deep p-6"><div className="text-xs uppercase tracking-widest text-muted-foreground">Transparent pricing</div><h2 className="mt-2 font-serif text-2xl">Starting at ${p.preparationFee.toFixed(2)} + mailing</h2><div className="mt-4 grid gap-3 md:grid-cols-4 text-sm"><div><b>Preparation</b><p>${p.preparationFee.toFixed(2)}</p></div><div><b>Included</b><p>{p.includedResponsePages} response sheets</p></div><div><b>Additional</b><p>${p.responsePagePrice.toFixed(2)}/response · ${p.supportingPagePrice.toFixed(2)}/supporting</p></div><div><b>Mailing</b><p>Standard ${p.standardMail.toFixed(2)} · Certified ${p.certifiedMail.toFixed(2)} · Registered ${p.registeredMail.toFixed(2)}</p></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">The exact total is calculated from the approved physical packet before payment. Supporting evidence is priced openly; there is no hidden page charge.</p></section>;}
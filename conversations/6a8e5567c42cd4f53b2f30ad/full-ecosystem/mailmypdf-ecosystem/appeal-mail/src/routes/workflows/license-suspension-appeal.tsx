import { createFileRoute } from "@tanstack/react-router";
import { LicenseSuspensionAppealWorkspace } from "@/components/workflow/license-suspension-appeal-workspace";
import { getWorkflow } from "@/domain/workflows";
export const Route=createFileRoute("/workflows/license-suspension-appeal")({head:()=>({meta:[{title:`${getWorkflow("license-suspension-appeal").title} — Appeal Mail`},{name:"description",content:getWorkflow("license-suspension-appeal").description}, { property: "og:title", content: `${getWorkflow("license-suspension-appeal").title} — Appeal Mail` }, { property: "og:description", content: getWorkflow("license-suspension-appeal").description }, { name: "twitter:card", content: "summary" }, { name: "twitter:title", content: `${getWorkflow("license-suspension-appeal").title} — Appeal Mail` }, { name: "twitter:description", content: getWorkflow("license-suspension-appeal").description }], links:[{rel:"canonical",href:"/workflows/license-suspension-appeal"}]}),component:LicenseSuspensionAppealWorkspace});

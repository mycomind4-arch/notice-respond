import {createFileRoute} from "@tanstack/react-router";import {useState} from "react";import {ADMINISTRATIVE_DECISION_APPEAL_PRICING} from "@/domain/administrative-decision-appeal-gold";
import { getWorkflowHeroImage } from "@/domain/workflow-hero-images";
export const Route=createFileRoute("/workflows/administrative-decision-appeal")({head:()=>({meta:[{title:"Appeal an Administrative Decision — Authority-First | Appeal Mail"},{name:"description",content:"Analyze an administrative decision, verify the controlling procedure, map evidence and contradictions, and prepare a human-reviewed response with transparent packet pricing."},{property:"og:title",content:"Appeal an Administrative Decision"},{property:"og:description",content:"Authority-first administrative decision appeal analysis with source verification, Gemini drafting, validation, pricing, and mailing proof."}],links:[{rel:"canonical",href:"/workflows/administrative-decision-appeal"}]}),component:Page});
function Page(){const[file,setFile]=useState<File|null>(null);const[status,setStatus]=useState("");const[result,setResult]=useState<any>(null);async function analyze(){if(!file)return;setStatus("Analyzing the administrative decision and verifying which procedural rules actually apply…");const f=new FormData();f.set("document",file);try{const r=await fetch("/api/workflows/administrative-decision-appeal/analyze",{method:"POST",body:f});const p=await r.json();if(!r.ok)throw new Error(p.error||"Unable to analyze decision.");setResult(p);setStatus("Analysis complete. Review authority, evidence gaps, contradictions, and unresolved procedure before drafting.")}catch(e){setStatus(e instanceof Error?e.message:"Unable to analyze decision.")}}return <main className="mx-auto max-w-6xl px-6 py-12"><section className="relative isolate overflow-hidden rounded-3xl bg-slate-950 p-8 hero-light shadow-xl"><p className="text-sm font-semibold uppercase tracking-[.22em] hero-muted">Administrative Decision Appeal • Authority-first</p><h1 className="mt-4 max-w-4xl text-4xl font-bold tracking-tight md:text-6xl">Appeal an Administrative Decision</h1><p className="mt-6 max-w-3xl text-lg leading-8 hero-muted">Start with the administrative decision or notice. We identify what the decision-maker actually decided, resolve the governing jurisdiction and authority sources, map disputed facts and evidence gaps, stress-test the response, and require your approval before payment and mailing.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{[["Authority raised","Decision-maker and jurisdiction are identified before procedural conclusions. Unsupported deadlines, forms, recipients, and exhaustion rules remain unresolved."],["Evidence-aware","Findings, disputed facts, cited authority, contradictions, timelines, and missing support are separated instead of invented."],["Transparent pricing","Preparation, response sheets, supporting sheets, mailing method, and packet surcharges are shown before checkout."]].map(([a,c])=><div key={a} className="rounded-2xl border hero-border hero-bg-glass p-5"><h2 className="font-semibold">{a}</h2><p className="mt-2 text-sm leading-6 hero-muted">{c}</p></div>)}</div></section><section className="mt-10 grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-3xl border border-slate-200 bg-white p-7"><h2 className="text-2xl font-semibold">What we analyze</h2><ol className="mt-6 space-y-4">{["Identify decision-maker, jurisdiction, decision date, reference numbers, findings, instructions, and cited authority.","Verify the controlling appeal path and timing from the notice and current authoritative sources.","Separate administrative findings and supported facts from disputed facts and assumptions.","Map evidence gaps, contradictions, timeline issues, and procedural uncertainty.","Draft and independently validate the response before explicit human approval for mailing."].map((x,i)=><li key={x} className="flex gap-4"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-slate-900 text-sm font-bold hero-light">{i+1}</span><span className="leading-7 text-slate-700">{x}</span></li>)}</ol></div><div className="rounded-3xl border border-slate-200 bg-slate-50 p-7"><h2 className="text-2xl font-semibold">Start with your decision</h2><p className="mt-3 text-sm leading-6 hero-divider">Starting price: <strong>${ADMINISTRATIVE_DECISION_APPEAL_PRICING.preparationFee.toFixed(2)}</strong> plus mailing, assuming {ADMINISTRATIVE_DECISION_APPEAL_PRICING.includedResponsePages} response pages and no supporting-document sheets.</p><input className="mt-6 block w-full rounded-xl border border-slate-300 bg-white p-3 text-sm" type="file" accept="application/pdf,image/png,image/jpeg" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="mt-4 w-full rounded-xl bg-slate-900 px-5 py-3 font-semibold hero-light disabled:opacity-40" disabled={!file} onClick={analyze}>Analyze administrative decision</button>{status&&<p className="mt-4 text-sm hero-divider">{status}</p>}{result&&<div className="mt-6 rounded-2xl bg-white p-5"><h3 className="font-semibold">Decision snapshot</h3><dl className="mt-3 space-y-2 text-sm"><div><dt className="inline font-medium">Issuer: </dt><dd className="inline">{result.analysis?.issuer||"Not identified"}</dd></div><div><dt className="inline font-medium">Jurisdiction: </dt><dd className="inline">{result.analysis?.jurisdiction||"Not identified"}</dd></div><div><dt className="inline font-medium">Deadline status: </dt><dd className="inline">{result.analysis?.deadlineStatus||"unverified"}</dd></div></dl></div>}</div></section><section className="mt-10 rounded-3xl border border-slate-200 bg-white p-7"><h2 className="text-2xl font-semibold">Transparent pricing</h2><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[["Preparation",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.preparationFee.toFixed(2)}`],["Included response",`${ADMINISTRATIVE_DECISION_APPEAL_PRICING.includedResponsePages} pages`],["Extra response",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.responsePagePrice.toFixed(2)}/sheet`],["Supporting evidence",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.supportingPagePrice.toFixed(2)}/sheet`],["Standard mail",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.standardMail.toFixed(2)}`],["Certified",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.certifiedMail.toFixed(2)}`],["Certified + return receipt",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.certifiedReturnReceipt.toFixed(2)}`],["Flat packet surcharge",`$${ADMINISTRATIVE_DECISION_APPEAL_PRICING.flatEnvelopeFee.toFixed(2)} when required`]].map(([a,b])=><div key={a} className="rounded-2xl bg-slate-50 p-4"><div className="text-sm text-slate-500">{a}</div><div className="mt-1 font-semibold">{b}</div></div>)}</div><p className="mt-6 text-sm leading-7 hero-divider">The exact price is calculated from the approved physical packet. Supporting-document sheets are billed separately when included.</p></section><p className="mt-10 text-sm text-slate-500">Appeal Mail is not a law firm and does not provide legal advice. Administrative procedures vary by jurisdiction and must be verified against current authoritative sources.</p></main>}

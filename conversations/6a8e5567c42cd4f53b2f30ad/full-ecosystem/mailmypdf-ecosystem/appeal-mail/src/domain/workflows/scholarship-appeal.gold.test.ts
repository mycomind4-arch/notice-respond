import {describe,expect,it} from "vitest";import{getWorkflow}from "./workflows";describe("Scholarship Appeal Gold workflow",()=>{it("has the canonical experience and document contract",()=>{const w=getWorkflow("scholarship-appeal");expect(w.experienceStages).toEqual(["understand","build","send"]);expect(w.acceptsDocuments).toBe(true);expect(w.primaryKeyword).toBe("scholarship appeal letter");expect(w.focusAreas).toEqual(expect.arrayContaining(["Award decision","Eligibility","Accomplishments","Supporting evidence"]));expect(w.workflowPrompt).toContain("scholarship");});});

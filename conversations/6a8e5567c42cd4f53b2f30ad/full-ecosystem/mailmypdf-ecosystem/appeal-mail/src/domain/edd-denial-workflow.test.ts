import {describe,expect,it} from "vitest";import{getWorkflow}from"./workflows";describe("EDD denial workflow",()=>{it("has California EDD-specific gold workflow metadata",()=>{const w=getWorkflow("edd-denial");expect(w.title).toContain("EDD");expect(w.primaryKeyword).toBe("appeal edd denial");expect(w.experienceStages).toEqual(["understand","build","send"]);expect(w.acceptsDocuments).toBe(true);expect(w.workflowPrompt).toMatch(/EDD|Employment Development Department|California/i);});});
import {createFileRoute}from"@tanstack/react-router";import{requireAuthenticatedUser,getSupabaseServer}from"@/platform/supabase";export const Route=createFileRoute("/api/workflows/administrative-decision-appeal/validate")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const{appealId}=await request.json();if(!appealId)return Response.json({error:"Appeal id is required."},{status:400});const s=await getSupabaseServer();const{data:a,error}=await s.from("appeals").select("*").eq("id",appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});if(!a.draft?.trim())return Response.json({error:"Draft is required before validation."},{status:409});const token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN,base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com";if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const c=await fetch(`${base.replace(/\/$/,"")}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"administrative-decision-appeal",task:"validation"})});const cfg=await c.json() as any;if(!c.ok||cfg.provider!=="gemini")throw new Error(cfg?.error||"Administrative Decision Appeal is currently configured for Gemini.");const prompt=cfg.promptOverride||`Act as an independent adversarial validator. Return JSON only. Check the draft against the source decision and cited authority. Flag invented facts, procedural claims, deadlines, forms, recipients, filing destinations, hearing rights, exhaustion rules, contradictions and unsupported evidence claims. JSON: {"score":0,"pass":false,"unsupportedClaims":[],"authorityProblems":[],"contradictions":[],"evidenceGaps":[],"deadlineProblems":[],"requiredCorrections":[]}\n${JSON.stringify({decision:a.decision,draft:a.draft})}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(cfg.model)}:generateContent?key=${encodeURIComponent(cfg.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{responseMimeType:"application/json",temperature:.1}})});const b=await r.json() as any;if(!r.ok)throw new Error(b?.error?.message||"Gemini validation failed.");const validation=JSON.parse(b.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||"").join("")||"{}");await s.from("appeals").update({review:{...validation,independentValidation:true,validatedAt:new Date().toISOString()},updated_at:new Date().toISOString()}).eq("id",appealId).eq("user_id",user.id);return Response.json({ok:true,appealId,validation,provider:"gemini",model:cfg.model});}catch(e){const m=e instanceof Error?e.message:"Unable to validate administrative appeal.";return Response.json({error:m},{status:/authentication|required|token/i.test(m)?401:502})}}}}});
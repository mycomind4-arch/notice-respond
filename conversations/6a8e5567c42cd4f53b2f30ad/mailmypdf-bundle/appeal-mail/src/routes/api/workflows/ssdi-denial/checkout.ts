import { createFileRoute } from "@tanstack/react-router";
import { requireAuthenticatedUser, getSupabaseServer } from "@/platform/supabase";
export const Route=createFileRoute("/api/workflows/ssdi-denial/checkout")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const input=await request.json() as {appealId?:string};if(!input.appealId?.trim())return Response.json({error:"Appeal id is required."},{status:400});const supabase=await getSupabaseServer();const{data:appeal,error}=await supabase.from("appeals").select("*").eq("id",input.appealId).single();if(error||!appeal)return Response.json({error:"Appeal case not found."},{status:404});if(appeal.user_id!==user.id)return Response.json({error:"You do not own this appeal case."},{status:403});if(appeal.workflow_id!=="ssdi-denial"||appeal.status!=="ready"||!appeal.review||!appeal.packet)return Response.json({error:"Appeal is not approved and ready for payment."},{status:409});const totalCents=Math.round(Number(appeal.packet.total||0)*100);if(!Number.isFinite(totalCents)||totalCents<=0)return Response.json({error:"Approved packet pricing is missing."},{status:409});const{default:Stripe}=await import("stripe");const secretKey=process.env.STRIPE_SECRET_KEY;if(!secretKey)return Response.json({error:"Stripe is not configured."},{status:503});const stripe=new Stripe(secretKey,{apiVersion:"2024-06-20" as Stripe.LatestApiVersion});const appUrl=process.env.APP_URL||"https://appeal-mail.pages.dev";const session=await stripe.checkout.sessions.create({mode:"payment",payment_method_types:["card"],line_items:[{price_data:{currency:"usd",product_data:{name:"SSDI appeal response packet",description:`${appeal.packet.responseSheets||1} response sheet(s) + ${appeal.packet.supportingSheets||0} supporting sheet(s) + ${appeal.packet.mailingMethod} mailing`},unit_amount:totalCents},quantity:1}],metadata:{appeal_id:appeal.id,workflow_id:"ssdi-denial",mailing_method:appeal.packet.mailingMethod,recipient_name:appeal.packet.recipientName,owner_user_id:user.id,total:appeal.packet.total},success_url:`${appUrl}/workflows/ssdi-denial?checkout=success&session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${appUrl}/workflows/ssdi-denial?checkout=cancelled`});return Response.json({ok:true,sessionId:session.id,url:session.url,total:appeal.packet.total,pricing:appeal.packet.pricing});}catch(error){const message=error instanceof Error?error.message:"Unable to create checkout session.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});

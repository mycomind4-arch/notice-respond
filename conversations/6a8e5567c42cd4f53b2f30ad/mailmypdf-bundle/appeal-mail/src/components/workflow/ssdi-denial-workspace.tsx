import { useState } from "react";
import { Upload, Sparkles, CheckCircle2, Send, FileText, AlertTriangle, ArrowRight } from "lucide-react";
import { workflows } from "@/domain/workflows";

const workflow = workflows["ssdi-denial"];
type Stage = "understand" | "build" | "send";
type Recipient={name:string;address1:string;address2:string;city:string;state:string;zip:string};
async function token(){const {getSupabaseClient}=await import("@/platform/supabase");const s=await getSupabaseClient();if(!s)throw new Error("Authentication is not configured.");const {data,error}=await s.auth.getSession();if(error||!data.session?.access_token)throw new Error("Please sign in to use this workflow.");return data.session.access_token;}
export function SsdiDenialWorkspace(){const[stage,setStage]=useState<Stage>("understand");const[file,setFile]=useState<File|null>(null);const[analysis,setAnalysis]=useState<any>(null);const[appealId,setAppealId]=useState("");const[draft,setDraft]=useState("");const[validation,setValidation]=useState("");const[recipient,setRecipient]=useState<Recipient>({name:"",address1:"",address2:"",city:"",state:"",zip:""});const[method,setMethod]=useState<"standard"|"certified"|"registered">("certified");const[review,setReview]=useState<any>();const[approved,setApproved]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const complete=recipient.name&&recipient.address1&&recipient.city&&recipient.state&&recipient.zip;
async function analyze(){if(!file)return;setBusy(true);setError("");try{const t=await token();const f=new FormData();f.append("document",file);const r=await fetch("/api/workflows/ssdi-denial/analyze",{method:"POST",body:f,headers:{authorization:`Bearer ${t}`}});const p=await r.json();if(!r.ok)throw new Error(p.error||"Analysis failed.");setAnalysis(p.analysis);setAppealId(p.appealId);setStage("build");}catch(e){setError(e instanceof Error?e.message:"Analysis failed.")}finally{setBusy(false)}}
async function build(){setBusy(true);setError("");try{const t=await token();const r=await fetch("/api/workflows/ssdi-denial/draft",{method:"POST",headers:{authorization:`Bearer ${t}`,"content-type":"application/json"},body:JSON.stringify({appealId,analysis})});const p=await r.json();if(!r.ok)throw new Error(p.error||"Drafting failed.");setDraft(p.draft||"");setValidation(p.validation||"");setStage("send");}catch(e){setError(e instanceof Error?e.message:"Drafting failed")}finally{setBusy(false)}}
async function approve(){setBusy(true);setError("");try{const t=await token();const r=await fetch("/api/workflows/ssdi-denial/approve",{method:"POST",headers:{authorization:`Bearer ${t}`,"content-type":"application/json"},body:JSON.stringify({appealId,draft,recipient,mailingMethod:method})});const p=await r.json();if(!r.ok){setReview(p.review);throw new Error(p.error||"Approval failed.")}setReview(p.review);setApproved(true);}catch(e){setError(e instanceof Error?e.message:"Approval failed")}finally{setBusy(false)}}
async function checkout(){setBusy(true);setError("");try{const t=await token();const r=await fetch("/api/workflows/ssdi-denial/checkout",{method:"POST",headers:{authorization:`Bearer ${t}`,"content-type":"application/json"},body:JSON.stringify({appealId})});const p=await r.json();if(!r.ok||!p.url)throw new Error(p.error||"Checkout failed.");window.location.assign(p.url);}catch(e){setError(e instanceof Error?e.message:"Checkout failed")}finally{setBusy(false)}}
return <main className="min-h-screen bg-paper px-6 py-10"><div className="mx-auto max-w-5xl"><header className="mb-10"><div className="text-xs uppercase tracking-[0.2em] text-muted-foreground">{workflow.title}</div><h1 className="mt-3 font-serif text-4xl md:text-5xl">Understand it. Build it. Send it.</h1><p className="mt-4 max-w-2xl text-base leading-7 text-muted-foreground">{workflow.description} Upload your SSA decision and let Gemini analyze the actual document.</p><div className="mt-6 h-1 rounded-full bg-muted"><div className="h-full bg-foreground" style={{width:stage==="understand"?"0%":stage==="build"?"50%":"100%"}}/></div><div className="mt-2 flex justify-between text-[11px] uppercase tracking-widest text-muted-foreground"><span>Understand</span><span>Build</span><span>Send</span></div></header>{error&&<div className="mb-6 flex gap-2 rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-800"><AlertTriangle size={16}/>{error}</div>}
{stage==="understand"&&<section className="rounded-2xl border border-rule bg-paper-deep p-8 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full border border-rule bg-paper"><Upload size={24}/></div><h2 className="mt-5 font-serif text-3xl">Upload your SSDI decision</h2><p className="mt-3 text-sm text-muted-foreground">PDF, PNG, or JPG. We analyze the actual document—no retyping required.</p><label className="mt-6 inline-flex cursor-pointer items-center gap-2 rounded-full bg-foreground px-5 py-3 text-sm text-background"><Upload size={16}/>Choose document<input type="file" accept="application/pdf,image/png,image/jpeg" className="sr-only" onChange={e=>setFile(e.target.files?.[0]||null)}/></label>{file&&<div className="mt-5 flex items-center justify-center gap-2 text-sm"><FileText size={16}/>{file.name}</div>}<button disabled={!file||busy} onClick={analyze} className="mt-6 inline-flex items-center gap-2 rounded-full border border-foreground px-5 py-3 text-sm disabled:opacity-40"><Sparkles size={16}/>{busy?"Analyzing…":"Analyze my decision"}</button></section>}
{stage==="build"&&analysis&&<section className="rounded-2xl border border-rule bg-paper-deep p-8"><h2 className="font-serif text-2xl">Here's what we found</h2><div className="mt-6 grid gap-4 md:grid-cols-3"><div className="rounded-xl border border-rule bg-paper p-5"><div className="text-[10px] uppercase tracking-widest text-muted-foreground">Decision</div><div className="mt-2 font-medium">{analysis.decision||"Needs review"}</div></div><div className="rounded-xl border border-rule bg-paper p-5"><div className="text-[10px] uppercase tracking-widest text-muted-foreground">Deadline</div><div className="mt-2 font-medium">{analysis.deadline||"Needs confirmation"}</div></div><div className="rounded-xl border border-rule bg-paper p-5"><div className="text-[10px] uppercase tracking-widest text-muted-foreground">Confidence</div><div className="mt-2 font-medium capitalize">{analysis.confidence||"Review required"}</div></div></div><div className="mt-6 rounded-xl border border-rule bg-paper p-6"><h3 className="font-serif text-xl">What matters</h3><p className="mt-3 whitespace-pre-wrap text-sm leading-7">{analysis.summary||"No summary returned."}</p></div>{analysis.reasons?.length&&<div className="mt-6 rounded-xl border border-rule bg-paper p-6"><h3 className="font-serif text-xl">Reasons for the decision</h3><ul className="mt-3 list-disc space-y-2 pl-5 text-sm">{analysis.reasons.map((x:string,i:number)=><li key={i}>{x}</li>)}</ul></div>}<button disabled={busy} onClick={build} className="mt-8 inline-flex items-center gap-2 rounded-full bg-foreground px-6 py-3 text-sm text-background disabled:opacity-40"><Sparkles size={16}/>{busy?"Building your appeal…":"Build my appeal"}<ArrowRight size={16}/></button></section>}
{stage==="send"&&<section className="rounded-2xl border border-rule bg-paper-deep p-8"><div className="flex items-center gap-3"><CheckCircle2 size={18}/><div><h2 className="font-serif text-2xl">Your SSDI appeal is ready for review</h2><p className="text-sm text-muted-foreground">Gemini drafted and independently checked the response. Nothing is mailed without approval.</p></div></div><textarea value={draft} onChange={e=>setDraft(e.target.value)} className="mt-8 min-h-[320px] w-full rounded-xl border border-rule bg-paper p-5 text-sm leading-7"/><div className="mt-6 rounded-xl border border-rule bg-paper p-6"><div className="text-[10px] uppercase tracking-widest text-muted-foreground">Independent validation</div><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{validation}</p></div><div className="mt-6 rounded-xl border border-rule bg-paper p-6"><h3 className="font-serif text-xl">Where should we send it?</h3><div className="mt-4 grid gap-3 md:grid-cols-2">{([["name","Recipient name"],["address1","Street address"],["address2","Apartment / suite"],["city","City"],["state","State"],["zip","ZIP code"]] as const).map(([k,l])=><input key={k} placeholder={l} value={recipient[k]} onChange={e=>setRecipient(r=>({...r,[k]:e.target.value}))} className="rounded-lg border border-rule bg-paper px-4 py-3 text-sm"/>)}</div><select value={method} onChange={e=>setMethod(e.target.value as typeof method)} className="mt-4 rounded-lg border border-rule bg-paper px-4 py-3 text-sm"><option value="standard">Standard</option><option value="certified">Certified</option><option value="registered">Registered</option></select></div>{review&&<div className="mt-6 rounded-xl border border-rule bg-paper p-6"><div className="text-[10px] uppercase tracking-widest text-muted-foreground">Readiness review</div><div className="mt-2 text-2xl font-semibold">{review.score}/100</div><p className="mt-2 text-sm text-muted-foreground">{review.issuesRequiringAttention} item(s) require attention.</p></div>}<div className="mt-6 flex flex-wrap gap-3"><button disabled={!complete||busy||approved} onClick={approve} className={`rounded-full border px-5 py-3 text-sm disabled:opacity-40 ${approved?"bg-foreground text-background":"border-foreground"}`}>{approved?"Approved and ready":busy?"Checking readiness…":"Approve & prepare to send"}</button><button disabled={!approved||busy} onClick={checkout} className="inline-flex items-center gap-2 rounded-full bg-foreground px-5 py-3 text-sm text-background disabled:opacity-40"><Send size={16}/>{busy?"Opening payment…":"Continue to payment"}</button></div></section>}</div></main>}

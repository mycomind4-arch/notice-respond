import test from "node:test";import assert from "node:assert/strict";import { getWorkflow } from "../src/domain/workflows";import { INSURANCE_DENIAL_LETTER_GOLD,INSURANCE_DENIAL_LETTER_PRICING as p } from "../src/domain/insurance-denial-letter-gold";
test("workflow 24 is defined for its intent",()=>{const w=getWorkflow("insurance-denial-letter");assert.equal(w.title,"Respond to an Insurance Denial Letter");assert.match(w.description,/denial letter/i);assert.match(w.workflowPrompt,/do not invent/i)});
test("workflow 24 pricing is transparent and margin-aware",()=>{assert.equal(INSURANCE_DENIAL_LETTER_GOLD.lifecycle,"authority");assert.ok(p.preparationFee>=12.99&&p.preparationFee<=39.99);assert.equal(p.includedResponsePages,3);assert.ok(p.responsePagePrice>=.35&&p.responsePagePrice<=.5);assert.ok(p.supportingPagePrice>=.2&&p.supportingPagePrice<=.35);assert.ok(p.standardMail>=4.99);assert.ok(p.certifiedMail>=10.99);assert.ok(p.registeredMail>p.certifiedMail)});
test("authority rules prohibit universal unsupported procedure",()=>{assert.ok(INSURANCE_DENIAL_LETTER_GOLD.authorityRules.some(x=>/Never invent/i.test(x)));assert.ok(INSURANCE_DENIAL_LETTER_GOLD.capabilities.includes("independent-validation"));assert.ok(INSURANCE_DENIAL_LETTER_GOLD.capabilities.includes("proof"))});
